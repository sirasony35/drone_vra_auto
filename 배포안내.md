# 드론 VRA 처방맵 웹앱 — 다른 PC 배포 안내

## 전달할 것 (폴더 통째로)

`drone_vra_auto` 폴더에서 아래 항목을 **하나의 폴더 구조 그대로** 전달한다.

```
drone_vra_auto/
├── 처방맵_웹앱_실행.bat      ← 실행 버튼
├── webapp/app.py             ← 웹앱 서버
├── operation_main.py         ← 처방 파이프라인 (핵심)
├── boundary_detector.py
├── vra_calculator.py
├── requirements.txt          ← 라이브러리 목록
├── 배포안내.md               ← 이 파일
└── vra_setting/              ← (선택) 설정 CSV 예시
```

> `data/`, `result/`, `webapp/jobs/` 등 대용량 데이터 폴더는 전달할 필요 없다.
> 사용자는 웹 화면에서 자기 파일을 업로드하므로 코드만 있으면 된다.

## 받는 PC에서 1회 설치 (약 10분)

1. **Miniconda 설치**: https://www.anaconda.com/download/success 에서
   "Miniconda Installers" → Windows 64-bit 다운로드 후 기본 옵션으로 설치.

2. **시작 메뉴 → "Anaconda Prompt (Miniconda3)" 실행** 후 아래 두 줄 입력:

   ```
   conda create -n vra -c conda-forge python=3.12 geopandas rasterio scipy scikit-image scikit-learn matplotlib flask -y
   ```

   (10분 정도 걸림. 완료되면 창을 닫아도 됨)

3. **`처방맵_웹앱_실행.bat` 더블클릭** → 검은 창이 뜨고 브라우저가 자동으로 열림.
   - bat가 `anaconda3`/`miniconda3`의 `python312` 또는 `vra` 환경을 자동으로 찾는다.
   - 다른 이름의 환경을 만들었다면 bat 파일을 메모장으로 열어 경로를 수정.

## 사용 방법

1. 검은 창(서버)이 켜진 상태에서 브라우저 화면에 3가지 업로드:
   - ① GNDVI 영상: `필지코드_..._GNDVI.tif` 또는 이것들을 묶은 zip
   - ② 바운더리: 필지별 zip 여러 개 선택 (또는 zip 묶음) — 없으면 자동 감지(정확도 낮음)
   - ③ VRA 설정 CSV: `field,total,spread,crop,grid_size,sigma,masking,height,width,drone_type`
2. "처방맵 생성 시작" 클릭 → 로그 확인 → 완료되면 결과 zip 다운로드.
3. **검은 창을 닫으면 웹앱이 종료**되므로 작업 중에는 열어 둘 것.

## 자주 나는 오류

| 증상 | 원인/해결 |
|---|---|
| "서버에 연결할 수 없습니다" | 검은 창(서버)이 꺼져 있음 → bat 다시 실행 |
| "Python env not found" (검은 창) | 2번 conda 환경 설치가 안 됨 → 위 설치 단계 수행 |
| "VRA 설정값을 찾을 수 없습니다" (로그) | CSV의 field 값과 영상 파일명 앞 필지코드가 불일치 |
| 특정 필지만 결과 없음 | 해당 필지가 CSV에 없거나, 바운더리 zip 파일명이 필지코드로 시작하지 않음 |

## 외부 접속 (Cloudflare Tunnel) — 채택된 방식

사무실 PC 한 대에 앱을 켜 두고, 외부 어디서든 브라우저로 접속하는 구성.

### 실행

1. 사무실 PC에서 **`처방맵_외부공개_실행.bat` 더블클릭**.
   - 웹앱 서버(최소화된 창) + Cloudflare Tunnel 창이 켜진다.
   - `cloudflared.exe`가 없으면 자동으로 다운로드한다 (최초 1회, 약 52MB).
2. 터널 창에 표시되는 **`https://xxxx.trycloudflare.com`** 주소를 팀원에게 공유.
3. 팀원은 아무 설치 없이 브라우저로 그 주소에 접속해 사용.
4. **두 창 모두 켜 둔 상태를 유지**해야 한다. 창을 닫으면 서비스 중단.

### 부팅 시 자동 시작 (선택)

`Win+R` → `shell:startup` → 열린 폴더에 `처방맵_외부공개_실행.bat`의 바로가기를 넣으면
PC를 켤 때마다 자동으로 시작된다.

### 알아둘 것

- **주소가 바뀜**: 무료 빠른 터널(trycloudflare.com)은 재시작할 때마다 주소가 바뀐다.
  주소를 고정하려면 Cloudflare 계정 + 회사 도메인을 등록해 "이름 있는 터널"로 전환
  (연 1~2만원 도메인 비용, 설정 별도).
- **보안**: 주소를 아는 사람은 누구나 사용할 수 있다(주소 자체가 무작위라 사실상의 암호 역할).
  더 안전하게 하려면 이름 있는 터널 + Cloudflare Access(무료, 이메일 인증)로
  지정한 사람만 접속하게 할 수 있다.
- **업로드 용량**: Cloudflare는 요청당 100MB 제한이 있지만, 웹앱이 파일을 64MB 조각으로
  나눠 올리도록 되어 있어 **GB급 파일도 문제없이 업로드된다** (171MB tif 실측 검증 완료).
- 업로드 속도는 사무실 인터넷의 업로드 대역폭을 따른다.

## 사내망 전용으로만 쓰는 경우 (대안)

외부 접속이 필요 없다면 `webapp/app.py` 마지막 줄 `host="127.0.0.1"`을 `host="0.0.0.0"`으로
바꾸고 방화벽에서 8000 포트를 허용하면, 같은 네트워크에서 `http://<그PC의IP>:8000`으로 접속할 수 있다.
